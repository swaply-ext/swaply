export interface NextSwap {
  // userId?: string;
  username?: string; //per la ruta /user/:username
  userAvatar: string;
  skillTitle: string;
  skillImage?: string;
  skillIcon?: string;
  distance: string;
  rating: number;
  isMatch: boolean;
}
